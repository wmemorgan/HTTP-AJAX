import styled from 'styled-components'
import { flex } from './StyleComponents/theme'

export default AppContainer = styled.div`
  width: 100%;
  max-width: 1000px;
  ${flex('column','center','center')};
  margin: 0 auto;
  
`